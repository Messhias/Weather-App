version: '3.1'

services:

  db:
    image: postgres
    ports:
      - 5432:5432
    restart: always
    environment:
      POSTGRES_USER: benestudio
      POSTGRES_DB: benestudio
      POSTGRES_PASSWORD: benestudio
